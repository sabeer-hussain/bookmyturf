# BookMyTurf — API Design (2.3)

## Overview

- **Style:** RESTful JSON API
- **Base URL:** `https://api.bookmyturf.in/v1`
- **Auth:** Bearer JWT token (access + refresh token pattern)
- **Response Format:** Standard envelope
- **Versioning:** URL-based (`/v1`)

---

## Standard Response Envelope

```json
// Success
{
  "success": true,
  "data": { ... },
  "meta": { "page": 1, "limit": 20, "total": 45 }
}

// Error
{
  "success": false,
  "error": {
    "code": "SLOT_NOT_AVAILABLE",
    "message": "Selected slot is already booked",
    "details": []
  }
}
```

---

## Authentication Headers

```
Authorization: Bearer <access_token>
X-Tenant-Id: <tenant_id>          // For tenant-scoped requests
```

---

## API Endpoints

### 1. Auth Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/auth/otp/send` | Public | — | Send OTP to phone number |
| POST | `/auth/otp/verify` | Public | — | Verify OTP & get tokens |
| POST | `/auth/google` | Public | — | Google OAuth login/signup |
| POST | `/auth/refresh` | Public | — | Refresh access token |
| POST | `/auth/logout` | Auth | All | Invalidate refresh token |
| GET | `/auth/me` | Auth | All | Get current user profile |

#### Request/Response Examples:

```json
// POST /auth/otp/send
{ "phone": "+919876543210" }

// POST /auth/otp/verify
{ "phone": "+919876543210", "code": "123456" }
// Response:
{
  "accessToken": "eyJ...",
  "refreshToken": "eyJ...",
  "user": { "id": "...", "role": "CUSTOMER", "firstName": "Rahul" }
}
```

---

### 2. Tenant Module (Platform Admin + Self-management)

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/tenants` | Auth | SUPER_ADMIN | Create new tenant |
| GET | `/tenants` | Auth | SUPER_ADMIN | List all tenants (paginated) |
| GET | `/tenants/:id` | Auth | SUPER_ADMIN, TURF_OWNER | Get tenant details |
| PATCH | `/tenants/:id` | Auth | SUPER_ADMIN, TURF_OWNER | Update tenant |
| DELETE | `/tenants/:id` | Auth | SUPER_ADMIN | Deactivate tenant |
| POST | `/tenants/onboard` | Auth | TURF_OWNER | Complete onboarding wizard |
| GET | `/tenants/slug/:slug` | Public | — | Check slug availability |

```json
// POST /tenants/onboard
{
  "businessName": "SportArena",
  "slug": "sportarena",
  "phone": "+919876543210",
  "city": "Mumbai",
  "state": "Maharashtra"
}
```

---

### 3. Users Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/users` | Auth | SUPER_ADMIN | List all users (platform-wide) |
| GET | `/users/:id` | Auth | All | Get user profile |
| PATCH | `/users/:id` | Auth | All (self) | Update own profile |
| DELETE | `/users/:id` | Auth | SUPER_ADMIN | Deactivate user |

---

### 4. Venues Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/venues` | Auth | TURF_OWNER | Create venue |
| GET | `/venues` | Auth | TURF_OWNER, MANAGER | List tenant's venues |
| GET | `/venues/:id` | Auth | TURF_OWNER, MANAGER | Get venue details |
| PATCH | `/venues/:id` | Auth | TURF_OWNER | Update venue |
| DELETE | `/venues/:id` | Auth | TURF_OWNER | Deactivate venue |

```json
// POST /venues
{
  "name": "SportArena Main",
  "address": "Andheri West",
  "city": "Mumbai",
  "state": "Maharashtra",
  "pincode": "400058",
  "openTime": "06:00",
  "closeTime": "23:00",
  "amenities": ["parking", "changing_room", "water", "floodlights"]
}
```

---

### 5. Courts Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/venues/:venueId/courts` | Auth | TURF_OWNER | Create court |
| GET | `/venues/:venueId/courts` | Auth | TURF_OWNER, MANAGER, STAFF | List courts in venue |
| GET | `/courts/:id` | Auth | TURF_OWNER, MANAGER, STAFF | Get court details |
| PATCH | `/courts/:id` | Auth | TURF_OWNER | Update court |
| DELETE | `/courts/:id` | Auth | TURF_OWNER | Deactivate court |

```json
// POST /venues/:venueId/courts
{
  "name": "Court A",
  "isIndoor": false,
  "surfaceType": "artificial_turf",
  "dimensions": "100x50 ft",
  "maxPlayers": 14
}
```

---

### 6. Court Sports Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/courts/:courtId/sports` | Auth | TURF_OWNER | Add sport to court |
| GET | `/courts/:courtId/sports` | Auth | TURF_OWNER, MANAGER | List sports for court |
| PATCH | `/court-sports/:id` | Auth | TURF_OWNER | Update sport config (price, duration) |
| DELETE | `/court-sports/:id` | Auth | TURF_OWNER | Remove sport from court |

```json
// POST /courts/:courtId/sports
{
  "sportId": "clx...",
  "baseSlotMinutes": 60,
  "pricePerSlot": 800,
  "peakPricePerSlot": 1200,
  "maxConsecutiveSlots": 3
}
```

---

### 7. Sports Module (Master Data)

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/sports` | Public | — | List all available sports |
| POST | `/sports` | Auth | SUPER_ADMIN | Add new sport |
| PATCH | `/sports/:id` | Auth | SUPER_ADMIN | Update sport |

---

### 8. Slot Configuration Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/court-sports/:courtSportId/slots` | Auth | TURF_OWNER, MANAGER | Create slot configs |
| GET | `/court-sports/:courtSportId/slots` | Auth | TURF_OWNER, MANAGER | Get slot configs |
| PATCH | `/slot-configs/:id` | Auth | TURF_OWNER, MANAGER | Update slot config |
| DELETE | `/slot-configs/:id` | Auth | TURF_OWNER, MANAGER | Delete slot config |
| POST | `/court-sports/:courtSportId/slots/bulk` | Auth | TURF_OWNER | Bulk create slots for week |

```json
// POST /court-sports/:courtSportId/slots/bulk
{
  "slots": [
    { "dayOfWeek": "MONDAY", "startTime": "06:00", "endTime": "07:00", "isPeakHour": false },
    { "dayOfWeek": "MONDAY", "startTime": "07:00", "endTime": "08:00", "isPeakHour": false },
    { "dayOfWeek": "MONDAY", "startTime": "18:00", "endTime": "19:00", "isPeakHour": true },
    { "dayOfWeek": "MONDAY", "startTime": "19:00", "endTime": "20:00", "isPeakHour": true }
  ]
}
```

---

### 9. Availability Module (Public — for booking page)

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/public/:tenantSlug/venues` | Public | — | List tenant's venues |
| GET | `/public/:tenantSlug/courts` | Public | — | List courts with sports |
| GET | `/public/:tenantSlug/availability` | Public | — | Get available slots for date |

```json
// GET /public/sportarena/availability?courtSportId=clx...&date=2026-06-20
// Response:
{
  "date": "2026-06-20",
  "courtSport": { "id": "clx...", "sportName": "Football", "pricePerSlot": 800 },
  "slots": [
    { "startTime": "06:00", "endTime": "07:00", "status": "AVAILABLE", "price": 800 },
    { "startTime": "07:00", "endTime": "08:00", "status": "BOOKED", "price": 800 },
    { "startTime": "18:00", "endTime": "19:00", "status": "AVAILABLE", "price": 1200 },
    { "startTime": "19:00", "endTime": "20:00", "status": "AVAILABLE", "price": 1200 }
  ]
}
```

---

### 10. Bookings Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/bookings` | Auth | CUSTOMER | Create booking (online — 10-min hold) |
| POST | `/bookings/walk-in` | Auth | TURF_OWNER, MANAGER, STAFF | Create walk-in booking (paid offline) |
| GET | `/bookings` | Auth | TURF_OWNER, MANAGER, STAFF | List bookings (tenant-scoped) |
| GET | `/bookings/my` | Auth | CUSTOMER | List customer's own bookings |
| GET | `/bookings/:id` | Auth | All | Get booking details |
| PATCH | `/bookings/:id/cancel` | Auth | CUSTOMER, TURF_OWNER, MANAGER | Cancel booking |
| PATCH | `/bookings/:id/complete` | Auth | TURF_OWNER, MANAGER, STAFF | Mark as completed |
| PATCH | `/bookings/:id/no-show` | Auth | TURF_OWNER, MANAGER, STAFF | Mark as no-show |
| PATCH | `/bookings/:id/mark-paid` | Auth | TURF_OWNER, MANAGER, STAFF | Mark walk-in as paid |

```json
// POST /bookings
{
  "courtSportId": "clx...",
  "bookingDate": "2026-06-20",
  "slots": [
    { "startTime": "18:00", "endTime": "19:00" },
    { "startTime": "19:00", "endTime": "20:00" }
  ],
  "notes": "Corporate match, need 2 footballs"
}
// Response:
{
  "id": "clx...",
  "totalAmount": 2400,
  "status": "PENDING",
  "razorpayOrderId": "order_xxx",
  "slots": [...]
}
```

---

### 11. Payments Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/payments/create-order` | Auth | CUSTOMER | Create Razorpay order for booking |
| POST | `/payments/verify` | Auth | CUSTOMER | Verify payment after Razorpay checkout |
| POST | `/payments/webhook` | Public | — | Razorpay webhook (signature verified) |
| GET | `/payments/:id` | Auth | TURF_OWNER, MANAGER | Get payment details |
| GET | `/payments` | Auth | TURF_OWNER, MANAGER | List payments (tenant-scoped) |
| POST | `/payments/:id/refund` | Auth | TURF_OWNER, MANAGER | Initiate refund |

```json
// POST /payments/create-order
{ "bookingId": "clx..." }
// Response:
{
  "razorpayOrderId": "order_xxx",
  "amount": 2400,
  "currency": "INR",
  "razorpayKeyId": "rzp_test_xxx"
}

// POST /payments/verify
{
  "razorpayOrderId": "order_xxx",
  "razorpayPaymentId": "pay_xxx",
  "razorpaySignature": "xxx"
}
```

---

### 12. Subscriptions Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/plans` | Public | — | List available subscription plans |
| GET | `/subscriptions/current` | Auth | TURF_OWNER | Get current subscription |
| POST | `/subscriptions` | Auth | TURF_OWNER | Subscribe to a plan |
| PATCH | `/subscriptions/upgrade` | Auth | TURF_OWNER | Upgrade/downgrade plan |
| POST | `/subscriptions/cancel` | Auth | TURF_OWNER | Cancel subscription |
| POST | `/subscriptions/webhook` | Public | — | Razorpay subscription webhook |

---

### 13. Staff Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/staff/invite` | Auth | TURF_OWNER | Invite staff (sends OTP link) |
| GET | `/staff` | Auth | TURF_OWNER, MANAGER | List staff members |
| PATCH | `/staff/:id/role` | Auth | TURF_OWNER | Update staff role |
| DELETE | `/staff/:id` | Auth | TURF_OWNER | Remove staff member |

```json
// POST /staff/invite
{
  "phone": "+919876543210",
  "firstName": "Amit",
  "role": "TURF_MANAGER"
}
```

---

### 14. Notifications Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/notifications` | Auth | All | List user's notifications |
| PATCH | `/notifications/:id/read` | Auth | All | Mark as read |
| PATCH | `/notifications/read-all` | Auth | All | Mark all as read |
| GET | `/notifications/unread-count` | Auth | All | Get unread count |

---

### 15. Analytics Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/analytics/overview` | Auth | TURF_OWNER, MANAGER | Revenue, bookings, occupancy summary |
| GET | `/analytics/revenue` | Auth | TURF_OWNER, MANAGER | Revenue trends (daily/weekly/monthly) |
| GET | `/analytics/bookings` | Auth | TURF_OWNER, MANAGER | Booking stats & trends |
| GET | `/analytics/peak-hours` | Auth | TURF_OWNER, MANAGER | Peak hours heatmap data |
| GET | `/analytics/sports` | Auth | TURF_OWNER, MANAGER | Sport-wise breakdown |
| GET | `/analytics/customers` | Auth | TURF_OWNER, MANAGER | Customer retention & top customers |

```json
// GET /analytics/overview?period=last_30_days
{
  "revenue": { "total": 145000, "change": 12.5 },
  "bookings": { "total": 180, "change": 8.2 },
  "occupancy": { "rate": 72.5, "change": 5.1 },
  "newCustomers": { "total": 45, "change": 15.0 }
}

// GET /analytics/peak-hours?venueId=clx...&period=last_30_days
{
  "heatmap": [
    { "day": "MONDAY", "hour": 18, "bookings": 28 },
    { "day": "MONDAY", "hour": 19, "bookings": 30 },
    { "day": "SATURDAY", "hour": 8, "bookings": 25 }
  ]
}
```

---

### 16. Cancellation Policy Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/cancellation-policy` | Auth | TURF_OWNER, MANAGER | Get current policy |
| PUT | `/cancellation-policy` | Auth | TURF_OWNER | Set/update cancellation policy |
| GET | `/public/:tenantSlug/cancellation-policy` | Public | — | View policy (customer-facing) |

```json
// PUT /cancellation-policy
{
  "policyType": "CUSTOM",
  "customRules": [
    { "hoursBeforeSlot": 24, "refundPercent": 100 },
    { "hoursBeforeSlot": 12, "refundPercent": 50 },
    { "hoursBeforeSlot": 6, "refundPercent": 25 },
    { "hoursBeforeSlot": 0, "refundPercent": 0 }
  ]
}
```

---

### 17. Super Admin Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/admin/dashboard` | Auth | SUPER_ADMIN | Platform overview stats |
| GET | `/admin/tenants` | Auth | SUPER_ADMIN | All tenants with filters |
| GET | `/admin/revenue` | Auth | SUPER_ADMIN | Platform revenue (commissions) |
| GET | `/admin/subscriptions` | Auth | SUPER_ADMIN | All subscriptions overview |
| PATCH | `/admin/tenants/:id/status` | Auth | SUPER_ADMIN | Activate/deactivate tenant |

---

### 18. Health & System

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| GET | `/health` | Public | — | Health check (DB, Redis, services) |
| GET | `/health/ready` | Public | — | Readiness probe for App Runner |

---

## Query Parameters (Common)

| Parameter | Type | Description | Example |
|-----------|------|-------------|---------|
| `page` | number | Page number (1-indexed) | `?page=1` |
| `limit` | number | Items per page (max 100) | `?limit=20` |
| `sort` | string | Sort field | `?sort=createdAt` |
| `order` | string | Sort direction | `?order=desc` |
| `search` | string | Search term | `?search=rahul` |
| `startDate` | string | Filter from date | `?startDate=2026-06-01` |
| `endDate` | string | Filter to date | `?endDate=2026-06-30` |
| `status` | string | Filter by status | `?status=CONFIRMED` |

---

## Rate Limiting

| Endpoint Group | Limit | Window |
|---------------|-------|--------|
| `/auth/otp/send` | 5 requests | per phone per 15 min |
| `/auth/*` | 20 requests | per IP per min |
| `/bookings` (POST) | 10 requests | per user per min |
| `/payments/*` | 20 requests | per user per min |
| General authenticated | 100 requests | per user per min |
| General public | 60 requests | per IP per min |

---

## Error Codes

| Code | HTTP Status | Description |
|------|-------------|-------------|
| `AUTH_INVALID_OTP` | 401 | OTP is incorrect or expired |
| `AUTH_MAX_ATTEMPTS` | 429 | Too many OTP attempts |
| `AUTH_TOKEN_EXPIRED` | 401 | Access token expired |
| `TENANT_NOT_FOUND` | 404 | Tenant doesn't exist |
| `TENANT_INACTIVE` | 403 | Tenant account deactivated |
| `VENUE_LIMIT_REACHED` | 403 | Subscription venue limit hit |
| `COURT_LIMIT_REACHED` | 403 | Subscription court limit hit |
| `SLOT_NOT_AVAILABLE` | 409 | Slot already booked |
| `SLOT_CONFLICT` | 409 | Overlapping booking detected |
| `BOOKING_NOT_CANCELLABLE` | 400 | Booking past cancellation window |
| `PAYMENT_VERIFICATION_FAILED` | 400 | Razorpay signature mismatch |
| `SUBSCRIPTION_EXPIRED` | 403 | Tenant subscription expired |
| `INSUFFICIENT_PERMISSIONS` | 403 | Role doesn't have access |
| `VALIDATION_ERROR` | 422 | Request body validation failed |
| `RATE_LIMIT_EXCEEDED` | 429 | Too many requests |

---

## Webhook Endpoints

### Razorpay Payment Webhook
```
POST /payments/webhook
Headers: X-Razorpay-Signature: <signature>
Events: payment.captured, payment.failed, refund.processed
```

### Razorpay Subscription Webhook
```
POST /subscriptions/webhook
Headers: X-Razorpay-Signature: <signature>
Events: subscription.activated, subscription.charged, subscription.cancelled
```

---

## API Security Checklist

- [x] JWT access tokens (15 min expiry)
- [x] Refresh token rotation (7 day expiry)
- [x] Rate limiting per endpoint group
- [x] Input validation on all endpoints (class-validator)
- [x] Tenant isolation on all tenant-scoped queries
- [x] Razorpay webhook signature verification
- [x] CORS restricted to frontend domains
- [x] Helmet.js security headers
- [x] SQL injection prevention (Prisma parameterized)
- [x] Request correlation ID for tracing
- [x] Audit logging for critical mutations

---

### 19. File Upload Module

| Method | Endpoint | Auth | Role | Description |
|--------|----------|------|------|-------------|
| POST | `/uploads/presigned-url` | Auth | TURF_OWNER, MANAGER | Get S3 presigned URL for upload |
| DELETE | `/uploads/:key` | Auth | TURF_OWNER | Delete uploaded file from S3 |

**Strategy:** Presigned URL (client uploads directly to S3, no backend file handling)

```json
// POST /uploads/presigned-url
{
  "fileName": "court-a.jpg",
  "fileType": "image/jpeg",
  "folder": "courts"     // "venues" | "courts" | "avatars" | "logos"
}
// Response:
{
  "uploadUrl": "https://bookmyturf-assets.s3.ap-south-1.amazonaws.com/...",
  "fileKey": "courts/clx123/court-a-1687345678.jpg",
  "publicUrl": "https://cdn.bookmyturf.in/courts/clx123/court-a-1687345678.jpg"
}
```

**Flow:**
1. Frontend requests presigned URL from backend
2. Frontend uploads file directly to S3 using presigned URL
3. Frontend saves returned `publicUrl` in venue/court/user form
4. CloudFront CDN serves images via `cdn.bookmyturf.in`

**Constraints:**
- Max file size: 5MB
- Allowed types: `image/jpeg`, `image/png`, `image/webp`
- Auto-resize via S3 Lambda trigger (optional Phase 2)
